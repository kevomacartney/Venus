set(VULKAN_DIR "Utility")

set(ENGINE_UTILITY_INC # include directories

        )

set(ENGINE_UTILITY_SRC # source directories

        )

set(ENGINE_VULKAN_CORE_SRC
        ${ENGINE_UTILITY_INC}
        ${ENGINE_UTILITY_SRC}
        )