
set(VULKAN_INC # include directories
        "vulkanUtility.h"
        )

set(VULKAN_SRC # source directories
        "vulkanUtility.cpp"
        )

set(VULKAN_PLUGIN_SRC
        ${VULKAN_INC}
        ${VULKAN_SRC}
        )